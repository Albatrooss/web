import React from 'react';
import styled from 'styled-components';
import { Player } from '../../../types';

interface Props {
  data: Player
}

const Hand: React.FC<Props> = ({ data }) => {

  return (
    <Wrapper>
      
    </Wrapper>
  )

}
export default Hand;

const Wrapper = styled.div`
  
`;
